---
type: source
title: AI DE 강의 1-06 Delta Lake와 ACID
aliases: [AI DE 1-06]
tags: [AI-DE-강의, 저장, 테이블-포맷]
created: 2026-09-14
updated: 2026-09-14
sources:
  - "raw/data-engineering/ai-de-course/part1/09. CH02-7. 데이터의 시간 여행 Delta Lake와 ACID 트랜잭션의 개념.pdf"
---

# AI DE 강의 1-06 Delta Lake와 ACID

[[AI 데이터 엔지니어링 강의]] Part 1의 저장 파트 마지막 강의. 데이터 레이크가 늪이 되는 기술적 원인과,
그것을 트랜잭션 로그로 해결하는 Delta Lake를 설명한다. 부제는 "데이터의 시간 여행".

## 인용

| 항목 | 내용 |
|---|---|
| 자료 | 패스트캠퍼스 AI 데이터 엔지니어링 강의 슬라이드, Part 1 |
| 덱 제목 | Delta Lake와 ACID 트랜잭션 (파일명: 데이터의 시간 여행 Delta Lake와 ACID 트랜잭션의 개념) |
| 원본 파일 | `part1/09. CH02-7. 데이터의 시간 여행 Delta Lake와 ACID 트랜잭션의 개념.pdf` (11p) |
| 강사 | 슬라이드에 표기 없음 |
| 작성 시기 | PDF 생성일 2026-02-19 |
| URL | 없음 (유료 강의 자료) |

## 요약

### 데이터 레이크가 늪이 되는 이유 (p5)

| 문제 | 설명 |
|---|---|
| 수정·삭제의 비효율 | DB가 아니라 파일 시스템이다. 한 줄을 고치려 해도 1GB 파일 전체를 다시 쓴다 |
| 동시성 | 쓰는 도중에 다른 사람이 읽으면 깨진 데이터를 읽는다. 격리가 없다 |
| 작은 파일 문제 | 스트리밍이 만든 수만 개의 작은 파일이 메타데이터 부하를 일으켜 쿼리가 느려진다 |
| 품질 | 스키마 강제가 없어 형식이 다른 데이터가 섞이고, 어느 버전이 최신인지 모른다 |
| 백업·복구 | 실수로 운영 데이터를 지우면 되돌릴 방법이 없다 |

### Data Lake vs Delta Lake (p6)

Delta Lake는 기존 스토리지(S3·HDFS·Azure Blob) 위에 **트랜잭션 로그라는 신뢰성 계층**을 얹는다.
ACID(all or nothing), Time Travel과 스냅샷(과거 시점 복구·감사), 스키마 강제로 오염 방지.

### ACID — 은행 송금으로 (p7)

| 성질 | 의미 | 은행 예 |
|---|---|---|
| 원자성 | 전부 성공하거나 전부 실행되지 않은 상태로 | 내 통장에서 빠졌는데 친구 통장에 입금이 안 됐다면 거래 전체를 취소 |
| 일관성 | 트랜잭션 전후에 정의된 규칙(스키마·제약)을 만족 | 잔액은 0원 이상, 없는 계좌로는 송금 불가 |
| 고립성 | 동시에 실행돼도 서로의 중간 상태를 못 본다 | 송금 완료 전 다른 사람이 조회하면 송금 전 잔액 |
| 내구성 | 커밋된 결과는 장애가 나도 보존 | "송금 완료" 직후 서버 전원이 꺼져도 내역이 남음 |

### 트랜잭션 로그 (p9)

| 항목 | 내용 |
|---|---|
| 위치 | 테이블 루트 아래 `_delta_log/` 폴더 |
| 형식 | 순차적으로 번호가 증가하는 JSON 파일 |
| 내용 | 메타데이터 변경, 파일 추가(Add)·삭제(Remove) |
| 상태 해석 | 로그를 순서대로 읽어 **현재 유효한 파일 목록**을 구성 |
| 동시성 제어 | 낙관적 동시성 제어(Optimistic Concurrency), 충돌을 감지하면 재시도 |

### 5가지 핵심 기둥 (p10)

트랜잭션 로그(모든 변경을 순서대로 기록하는 진실의 원천) · 스냅샷과 체크포인트(로그를 요약해 빠른 읽기와
Time Travel의 기준점) · 스키마 관리(강제로 오염 방지, 진화로 유연한 변경) · 데이터 최적화(작은 파일 병합,
Z-Ordering) · 오픈 스토리지(Parquet 파일로 저장, 벤더 비종속).

## 핵심

- 설계의 요점은 **데이터 파일은 그대로 두고 "지금 테이블을 이루는 파일이 무엇인가"를 로그로 관리한다**는 것이다.
  그러면 원자성은 로그 한 건의 커밋이 되고, Time Travel은 로그를 과거 버전까지만 읽는 일이 된다.
  *(덱의 "로그를 순서대로 읽어 현재 유효한 파일 목록"에서 위키가 정리한 것)* → [[Delta Lake]]
- [[AI DE 강의 1-02 AI DE 마인드셋 Latency와 Versioning]]이 요구한 "학습 시점의 데이터 스냅샷"을 대용량에서
  복사 없이 제공하는 수단이다. → [[데이터와 모델 버전 관리]]
- 레이크가 늪이 되는 **기술적** 원인에 대한 답이다. **조직적** 원인(위치·소유자·정의 불명)은
  [[AI DE 강의 1-15 데이터 거버넌스와 카탈로그]]가 다룬다. → [[데이터 레이크하우스]]

## 주의·결함

- **로그 파일 이름** — 덱은 `000000.json`으로 쓰지만, 실제 커밋 파일은 버전을 **20자리로 0-패딩**한
  `00000000000000000000.json`이다. [Delta Lake PROTOCOL.md https://github.com/delta-io/delta/blob/master/PROTOCOL.md ,
  2026-09-14 확인]
- **"Compaction(압축)"** — compaction은 작은 파일을 **합치는** 것(병합)이지 압축(compression)이 아니다. 같은
  코스의 [[AI DE 강의 1-05 열 기반 저장 Parquet와 Avro]]는 "작은 파일 병합(Compaction)"으로 바르게 쓴다.
- 운영상의 한계는 다루지 않는다: 체크포인트 주기(참조 구현의 기본값은 10커밋마다), 오래된 데이터 파일을 정리하면
  Time Travel로 갈 수 있는 과거가 제한된다는 점.
- 11p 중 3장이 섹션 타이틀이고, 마무리 슬라이드(p11)는 제목뿐이다.

## 관련

- 개념·도구: [[Delta Lake]] · [[데이터 레이크하우스]] · [[데이터와 모델 버전 관리]]
- 이전 강의: [[AI DE 강의 1-05 열 기반 저장 Parquet와 Avro]]
- 다음 강의: [[AI DE 강의 1-07 배치 처리와 ETL·ELT]]
